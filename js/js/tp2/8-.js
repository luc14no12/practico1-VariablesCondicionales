let numero = parseInt(prompt("ingrese un numero y le diremos si es divisible por 2"));


if(numero % 2 === 0){

console.log("El numero" + " " + numero + " " + "es divisible por 2")
}else{

console.log("El numero" + " " + numero + " " + "no es divisible por 2"  )
 
}
